# Output 阶段采样差异深度分析

**实验目的**：找到 reasoning 阶段路径不同，但 output 阶段仍保持相同前缀、最终在 output 阶段发生分歧的案例，与 [reasoning 阶段分歧分析](20260322_glm5_temperature_sampling.md) 形成对照。

---

## 实验设置

| 参数 | 值 |
|------|----|
| **模型** | GLM-5 (EAGLE speculative decoding) |
| **Prompt** | `你好` |
| **Temperature** | 0.8 |
| **请求次数** | 2（相同输入，独立采样） |

---

## 两次输出对比

### 请求 1（`7dbd9cff07c64b7b...`）

**Reasoning**：
```
收到用户发来的"你好"这样简单的问候，需要礼貌地回应并引导对话。首先，我应该以热情友好的态度回应问候，这能让用户感受到被重视。同时，考虑到这是初次互动，需要简要介绍自己的身份和功能，让用户了解我能提供哪些帮助。最后，应该主动询问用户需要什么帮助，以开放式问题引导对话继续进行。这样的回应既保持了礼貌，又为后续交流铺平了道路。
```

**Output**（100 字符）：
```
你好！我是GLM，Z.ai训练的大语言模型。很高兴能与你交流！

我可以回答问题、提供信息或协助你完成各种语言任务。无论是学习、工作还是日常生活中的疑问，我都很乐意帮忙。

请问有什么我能为你做的吗？
```

**EAGLE**：59 cycles | Accept rate 49.7% | Avg 1.49/cycle

### 请求 2（`b014abd3e3f645d7...`）

**Reasoning**：
```
收到用户发来的"你好"这样简单的问候，需要礼貌地回应并引导对话。首先，我应该以热情友好的态度回应用户的问候，这能让对话有一个良好的开端。接着，为了更好地服务用户，我需要主动询问他们需要什么帮助，这样可以引导用户表达他们的具体需求。同时，我要保持开放和专业的态度，让用户感受到我是可以信赖的助手。这样的回应既展现了礼貌，又为后续的对话打开了可能性。
```

**Output**（73 字符）：
```
你好！我是GLM，Z.ai训练的大语言模型。很高兴能与你交流！

我可以回答问题、提供信息或协助你完成各种语言任务。请告诉我今天我能为你做些什么？
```

**EAGLE**：60 cycles | Accept rate 42.8% | Avg 1.28/cycle

---

## 两阶段分歧对比

| 阶段 | 分歧位置 | 共同前缀 |
|------|---------|---------|
| **Reasoning（token 级）** | 第 27 个 token | `收到用户发来的"你好"这样简单的问候，需要礼貌地回应并引导对话。首先，我应该以热情友好的态度` |
| **Reasoning（字符级）** | 第 48 个字符 | `收到用户发来的"你好"这样简单的问候，需要礼貌地回应并引导对话。首先，我应该以热情友好的态度回应` |
| **Output（token 级）** | 第 32 个 token | `你好！我是GLM，Z.ai训练的大语言模型。很高兴能与你交流！

我可以回答问题、提供信息或协助你完成各种语言任务。` |
| **Output（字符级）** | 第 58 个字符 | `你好！我是GLM，Z.ai训练的大语言模型。很高兴能与你交流！

我可以回答问题、提供信息或协助你完成各种语言任务。` |

**关键数字**：Reasoning 在第 27 个 token 就分歧，而 output 直到第 32 个 token 才分歧——output 的共同前缀比 reasoning **长了 5 个 token**。

---

## Reasoning 阶段分歧

### 分歧 token（Reasoning 第 27 个 token）

| | Token | 文本 | 上下文 |
|-|-------|------|--------|
| 请求 1 | `103496` | `回应` | `...应该以热情友好的态度回应问候，这能让用户感受到被重视。...` |
| 请求 2 | `98566` | `回` | `...应该以热情友好的态度回应用户的问候，这能让对话有一个良...` |

### 思路差异（尽管措辞不同，核心策略相似）

| 维度 | 请求 1 | 请求 2 |
|------|--------|--------|
| 问候方式 | 热情友好回应问候 | 热情友好回应用户的问候 |
| 策略侧重 | 简要介绍身份和功能 → 再询问需求 | 先询问需求 → 引导用户表达 |
| 结果 | **两条 reasoning 路径的结论非常相似**，都倾向于介绍自己 + 询问需求 |

尽管 reasoning 在第 27 个 token 就分歧，**两条思路最终都收敛到了类似的行动计划**，这解释了为什么 output 前 32 个 token 完全相同。

---

## Output 阶段分歧（核心）

### 共同 Output 前缀（32 tokens，字符级 58 字符）

```
你好！我是GLM，Z.ai训练的大语言模型。很高兴能与你交流！

我可以回答问题、提供信息或协助你完成各种语言任务。
```

### 分歧 token（Output 第 32 个 token）

两次请求在 output 阶段的第 32 个 token 产生分歧，此时 **logits 分布不同**（因为 reasoning 路径不同，KV cache 已经不同）：

**请求 1 — Cycle 3591 的 Logits 分布**：

| Rank | Token | Token ID | Probability |
|------|-------|----------|-------------|
| 1 | `我会` | 103505 | 0.5667 (56.7%) |
| 2 | `无论是` | 102784 | 0.1840 (18.4%) |
| 3 | `请问` | 110758 | 0.1116 (11.2%) |
| 4 | `无论` | 100426 | 0.0985 (9.8%) |
| 5 | `请` | 98964 | 0.0133 (1.3%) |
| 6 | `有什么` | 101665 | 0.0133 (1.3%) ← **SELECTED** |

Shannon Entropy: **1.7621 bits**

**请求 2 — Cycle 6166 的 Logits 分布**：

| Rank | Token | Token ID | Probability |
|------|-------|----------|-------------|
| 1 | `我会` | 103505 | 0.6967 (69.7%) |
| 2 | `请` | 98964 | 0.0734 (7.3%) |
| 3 | `请问` | 110758 | 0.0572 (5.7%) |
| 4 | `你想` | 106949 | 0.0393 (3.9%) |
| 5 | `你会` | 103318 | 0.0347 (3.5%) |
| 6 | `你需要` | 114468 | 0.0186 (1.9%) |

Shannon Entropy: **1.3345 bits**

### Logits 分布对比

两次请求在 output 分歧点的 logits **不同**（与 [reasoning 阶段分歧案例](20260322_glm5_temperature_sampling.md) 正好相反）：

| 对比维度 | Reasoning 分歧案例 | Output 分歧案例（本文） |
|---------|-------------------|----------------------|
| 分歧发生阶段 | Reasoning 阶段 | Output 阶段 |
| 分歧点 logits | **完全相同** | **不同**（KV cache 已分叉） |
| 随机性来源 | 纯粹随机数种子 | 随机数种子 + 不同 KV cache |
| 两条路径关系 | 同一 logits 下的不同采样 | 不同 logits 下的不同采样 |

**关键洞察**：当分歧发生在 output 阶段时，尽管 reasoning 路径不同导致 KV cache 不同，两次请求仍能保持相同的 output 前缀（58 字符），说明不同的 reasoning 路径可以"收敛"到相同的 output 策略。一旦 output 阶段 logits 分布发生显著变化，分歧才真正体现在用户可见的输出上。

---

## 两种分歧案例对比总结

| | [案例一（Reasoning 分歧）](20260322_glm5_temperature_sampling.md) | 案例二（Output 分歧，本文） |
|-|---------------------|------------------|
| **Prompt** | `你好` | `你好` |
| **Reasoning 分歧位置** | 第 44 个 token | 第 27 个 token |
| **Output 分歧位置** | 第 3 个字符 | 第 58 个字符 |
| **Output 共同前缀** | `你好！` (3字符) | `你好！我是GLM，Z.ai训练的大语言模型。很高兴能与你交流！

我可以回答问题、提供信息或协助你完成各种语言任务。` (58字符) |
| **分歧点 logits** | 两次请求完全相同 | 两次请求不同 |
| **Entropy at divergence** | 1.40 bits | 1.76 bits (req1) / 1.33 bits (req2) |
| **因果关系** | Reasoning 分歧 → Output 分歧 | Reasoning 分歧，但 Output 先"收敛"再分歧 |

---

*本报告由 `logits_analyzer` 生成，数据来源：GLM-5 + EAGLE speculative decoding，temperature=0.8*
